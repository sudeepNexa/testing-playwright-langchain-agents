# Regression specs

SDET-authored Playwright Test specs land here, one file per explored live URL
or flow. Each spec is committed on its own branch and delivered as a pull
request; see `skills/regression-authoring` in the NexAI agent project for the
authoring and delivery workflow.

Run everything under this directory:

```bash
npm run test:regression
```
